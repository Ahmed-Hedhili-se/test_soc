# Core LLM / Agent frameworks
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-ollama>=0.1.0
langchain-huggingface>=0.0.3   # replaces deprecated langchain_community.embeddings.HuggingFaceEmbeddings
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=1.0.0

# Vector stores & embeddings
chromadb>=0.5.0
langchain-chroma>=0.1.4        # replaces deprecated langchain_community.vectorstores.Chroma
sentence-transformers>=3.0.0

# API / Networking
fastapi>=0.111.0
uvicorn>=0.30.0
python-multipart>=0.0.9        # required by FastAPI for form/file uploads
httpx>=0.27.0
requests>=2.32.0

# Data validation & Parsing
pydantic>=2.7.0
PyYAML>=6.0.1

# Testing (dev)
pytest>=8.0.0

# DPO fine-tuning (continual improvement loop -- training/dpo_train.py)
# Optional: train_role() degrades to a clear skip message if these aren't
# installed, so the rest of the pipeline works without them.
torch>=2.2.0
transformers>=4.42.0
trl>=0.9.4
datasets>=2.19.0
accelerate>=0.30.0
